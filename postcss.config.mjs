const config = {
  theme: {
    extend: {
      colors: {
        primary: '#FF5733', // Orange  – Fun & energetic
        accent: '#FFD700' ,// (Hover effects, badges, highlights) 
        secondary: '#FFD700',// text
        background: '#F0F8FF' //light blue

      }
    
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
